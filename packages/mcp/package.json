{
  "name": "@maestro/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:orchestrator": "esbuild src/orchestrator/index.ts --bundle --platform=node --format=esm --outfile=dist/orchestrator.js --external:@modelcontextprotocol/sdk",
    "build": "pnpm build:orchestrator"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
